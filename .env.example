# ALLOWED_HOSTS setting
# List of hosts/domain names that this Django site can serve.
# 
# Using a leading dot (e.g., '.yourdomain.com') allows all subdomains of 
# 'yourdomain.com' to be valid, including 'www.yourdomain.com', 'api.yourdomain.com', etc.
# Without the leading dot, only the exact domain name is allowed.

ALLOWED_HOSTS=localhost,127.0.0.1,.yourdomain.com

# Database settings
DATABASE_URL=

# Secret
DJANGO_SECRET_KEY=
